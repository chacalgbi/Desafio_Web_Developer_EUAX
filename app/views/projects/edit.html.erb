<h1><%= t('labels.editing_project') %></h1>

<%= render 'form', project: @project %>

<%= link_to t('links.show'), @project, class:"btn btn-primary" %> |
<%= link_to t('links.back'), projects_path, class:"btn btn-primary" %>
