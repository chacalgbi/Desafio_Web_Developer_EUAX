<h1>Editing Activity</h1>

<%= render 'form', activity: @activity %>

<%= link_to t('links.show'), @activity, class:"btn btn-primary" %> |
<%= link_to t('links.back'), projects_path, class:"btn btn-primary" %>
